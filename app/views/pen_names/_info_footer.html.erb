<span class="footer">
  <span class="timestamp text-muted">
    <%= timestamp(pen_name) %>
  </span>

  <span class="counter text-muted">
    (
      <% if pen_name.user == current_user %>
        <%= "user_notes: #{pen_name.user_notes.count}" %>,
      <% else %>
        <%= "user_notes: #{pen_name.user_notes.where(status: Note::ST_OPEN).count}" %>,
      <% end %>
      <%= "groups: #{pen_name.groups.count}" %>
    )
  </span>

  <span class="command">
    <% if kind == "show" and pen_name.user == current_user %>
      <%= link_to "[delete]", pen_name,
                  method: :delete, data: { confirm: "You sure?" } %>  

      <% if pen_name.is_open? %>
        <%= link_to "[to_close]", to_close_pen_name_path(pen_name) %>
      <% else %>
        <%= link_to "[to_open]", to_open_pen_name_path(pen_name) %>
      <% end %>

      <%= link_to "[edit]", edit_pen_name_path(pen_name) %>
    <% end %>
  </span>
</span>