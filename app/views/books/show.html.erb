<% provide(:title, @book.title) %>
<div class="row">

  <aside class="col-md-4">

    <%= render "info" %>

  </aside>

  <div class="col-md-8">

    <span class="header">
      <span class="count">
        pages (<%= @book.pages.count %>)
      </span>
      <span class="new_command">
      </span>
    </span>

    <% if @book.pages.any? %>
      <ol class="notes">
        <%= render @pages %>
      </ol>
      <%= will_paginate @pages %>
    <% end %>
  </div>

</div>